package A
  part B
    let x in real [W]
    let y in integer [W] 
  part

  requirement R1 is
    all (B::x in real), B::y > 10 end
  requirement

  requirement R2 is
    any (B::x in real), B::y > 10, B::x = 520 end
  requirement

package
